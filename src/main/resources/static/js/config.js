var primary = localStorage.getItem("primary") || '#534686';
var secondary = localStorage.getItem("secondary") || '#FFA47A';
var success = localStorage.getItem("success") || '#67C100';
var info = localStorage.getItem("info") || '#3EBCB9';
var warning = localStorage.getItem("warning") || '#E56809';
var danger = localStorage.getItem("danger") || '#DC0808';


window.KohoAdminConfig = {
	// Theme Primary Color
	primary: primary,
	// theme secondary color
	secondary: secondary,
	// theme success color
	success: success,
	// theme info color
	info: info,
	// theme warning color
	warning: warning,
	// theme danger color
	danger: danger,
};
